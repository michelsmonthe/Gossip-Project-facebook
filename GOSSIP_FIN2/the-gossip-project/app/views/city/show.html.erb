<!DOCTYPE html>
<html>
    <br/>
    <br/>
    <div class="jumbotron jumbotron-fluid" style="background-color:#cae3ed">
      <div class="container">
        <h1 class="display-5 text-center" style="color:#37444a" ><%= @city.name  %></h1>
        <p class="text-center">
         
         Voici les gossip de la ville n° <%=@city.id%><br><br>
         <a href="<%=gossip_index_path %>"><button class='btn btn-primary'>Retour</button></a>
        </p>
      </div>
    </div>

    <h3 class="display-5 text-center" style="color:#37444a">Voici la liste des gossips postés dans la ville de <%= @city.name %></h3>
		<%@city.users.each do |user|%>
			<% user.gossips.each do |gossip| %>
			<div class="list-group" style="margin-left: 15px; margin-right: 15px">
			   <a href="<%= gossip_path(gossip.id)%>" class="list-group-item list-group-item-action" style="background-color:#cae3ed">
			        <div class="d-flex w-100 justify-content-between">

			            <h5 class="mb-1"><%= gossip.title %></h5>
			            <small>Gossip n°<%= gossip.id %></small>
			        </div>
			          <p class="mb-1"><%=gossip.user.first_name%> <%=gossip.user.last_name%></p>
			          <small><%= gossip.created_at %></small>
			    </a>   
			</div>

			  
			<br/><br/>
			<% end %>
		<% end %>
</html>



